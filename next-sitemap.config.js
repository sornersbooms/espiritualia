/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://espiritualia-three.vercel.app', // URL temporal hasta que tengas dominio propio
    generateRobotsTxt: true, // (optional)
    generateIndexSitemap: false,
    // ...other options
}
